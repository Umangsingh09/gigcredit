name: worker_app
description: "GigCredit Worker Mobile & Web App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^8.2.1
  fl_chart: ^1.2.0
  intl: ^0.20.3
  percent_indicator: ^4.2.5
  provider: ^6.1.5+1
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
