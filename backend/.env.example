# Supabase Auth password registration and login
SUPABASE_URL=
SUPABASE_KEY=

# development | production. Defaults to "production" when unset. Only
# "development" allows the local-<email> auth fallback in
# backend/app/api/deps.py when Supabase is not configured. Never set
# this to "development" in a real deployment.
ENVIRONMENT=
